#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define mn min
#define mx max
//  hare Krishna //
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int tc;
    cin >> tc;
    while (tc--)
    {
        int k,q;
        cin >> k>>q;
       vector<int>a,b;
       ll sum1=0,sum2=0;
        for (int i = 0; i < k; i++)
        {
           int ai;
           cin>>ai;
           a.push_back(ai);
        }
        for (int i = 0; i < q; i++)
        {
            int ai;
            cin >> ai;
           
            b.push_back(ai);
        }
        
    }
}