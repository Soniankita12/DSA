#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int tc;
    cin >> tc;
    while (tc--)
    {
        
        int p1,p2,p3;
        cin>>p1>>p2>>p3;
        if(p1== p2 && p2 == p3 )
        {
          cout<<"-1"<<"\n";
        }
    }
}